//variables
var cont = 0;
const rndInt = [];

//Randomizer
dices.forEach(randomizer);
function randomizer(dice) {
  cont ++;
  rndInt[cont] = Math.floor(Math.random() * dice) + 1;
}

//Randomizer with min and max numbers
function minMaxRandomizer(min, max) {
    return Math.floor(Math.random() * (max - min) + min);
}
console.log("Randomized");
