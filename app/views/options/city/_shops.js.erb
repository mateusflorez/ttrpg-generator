res = "A cidade possui os seguintes comércios:";
rand = minMaxRandomizer(1, 20);
if (rand > 5) {
    res += "§• Pousada/Taverna. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 9) {
    res += "§• Suprimentos de aventura. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 8) {
    res += "§• Animais e Montarias. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 14) {
    res += "§• Livros e mapas. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 17) {
    res += "§• Comerciante de joias e gemas. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 13) {
    res += "§• Armeiro. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Banco. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 14) {
    res += "§• Funileiro. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 10) {
    res += "§• Alfaiate. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Poções, venenos, ervas. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Ídolos religiosos e bênçãos. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 9) {
    res += "§• Vendedor de comida e bebida. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 11) {
    res += "§• Templo (Rituais, funerais, dar nomes, casamentos). "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 16) {
    res += "§• Tomos e pergaminhos de feitiços. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Suprimentos de roubo. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Loja de armas. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Veículos e transporte. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Guilda de Aventureiros. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 18) {
    res += "§• Itens Mágicos. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 7) {
    res += "§• Ferreiro. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 17) {
    res += "§• Necromancia/Ressurreição. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 16) {
    res += "§• Correios. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Bordel. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 12) {
    res += "§• Vendas de terrenos. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 9) {
    res += "§• Carpinteiro. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Guilda de bardos. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 11) {
    res += "§• Curandeiro/Médico. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Construção de barcos (deve ser costeira). "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 15) {
    res += "§• Sindicato de Trabalhadores (qualquer tipo). "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 11) {
    res += "§• Pedreiro. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 17) {
    res += "§• Universidade. "+merchantQualityRandomizer();
}
rand = minMaxRandomizer(1, 20);
if (rand > 16) {
    res += "§• Mercenários. "+merchantQualityRandomizer();
}
