$(function(){

    //Set dice
    const dices = [100];

    //Randomize
    <%= render :partial => 'options/shared/randomizer' %>

    //Option selector
    var res;
    switch (<%= @resp %>) {
        //Mission
        case 1:
            <%= render :partial => 'options/missions/mission' %>
            break;
        //Source
        case 2:
            <%= render :partial => 'options/missions/source' %>
            break;
    }

    //Set history
    <%= render :partial => 'options/shared/set_history' %>

    //Save
    <%= render :partial => 'options/shared/save' %>

    //Print
    <%= render :partial => 'options/shared/print' %>

});
