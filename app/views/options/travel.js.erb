$(function(){

    //Set dice
    var dice = 20;

    //Randomize
    <%= render :partial => 'options/shared/randomizer' %>

    //Option selector
    var res;
    switch (<%= @resp %>) {
        //climate
        case 1:
            if (rndInt === 1){
                res = "Geada/Nevasca"
            } else if (rndInt > 1 && rndInt < 5){
                res = "Chuvoso"
            } else if (rndInt > 4 && rndInt < 9){
                res = "Nublado"
            } else if (rndInt > 8 && rndInt < 12){
                res = "Cáu limpo"
            } else if (rndInt > 11 && rndInt < 19){
                res = "Claro/Quente"
            } else {
                res = "Muito quente"
            }
            break;
        case 2:
            res = rndInt-4;
            break;
        case 3:
            res = rndInt-2;
            break;
    }

    //Set history
    <%= render :partial => 'options/shared/set_history' %>

    //Save
    <%= render :partial => 'options/shared/save' %>

    //Print
    <%= render :partial => 'options/shared/print' %>

});
