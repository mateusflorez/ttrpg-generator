if (rndInt[1] < 52) {
  res = "Pântano contínuo.";
} else if (rndInt[1] > 51 && rndInt[1] < 59) {
  res = "Via navegável.";
} else if (rndInt[1] > 25 && rndInt[1] < 66) {
  res = "Lago. "+lakeRandomizer();
} else if (rndInt[1] > 65 && rndInt[1] < 73) {
  res = "Bosque Pequeno. "+woodRandomizer();
} else if (rndInt[1] > 72 && rndInt[1] < 80) {
  res = "Afloramento rochoso. "+rockyoutcropRandomizer();
} else if (rndInt[1] > 80 && rndInt[1] < 87) {
  res = "Assentamento não marcado.";
} else if (rndInt[1] > 86 && rndInt[1] < 94) {
  res = "rolo D6. 1-2: Estrutura / 3-4: Monumento / 5-6: Interessante elemento paisagístico.";
} else {
  res = "Covil de Monstro.";
}
