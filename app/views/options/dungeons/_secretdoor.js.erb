function sdRandomizer() {
    rand = minMaxRandomizer(1, 6);
    var sd = "";
    if (rand < 3) {
        rndInt[7] += 40;
        sd = "A porta secreta se abre para uma sala. ";
    } else if (rand < 2 && rand < 5) {
        rndInt[3] += 40;
        sd = "A porta secreta abre para a passagem.";
    }  else {
        temp1 = minMaxRandomizer(1, 100);
        var temp = "";
        if (temp1 < 51) {
            rndInt[3] += 50;
            temp += "passagem. "+passageRandomizer();
        } else {
            rndInt[7] += 50
            temp += "sala. "+roomRandomizer();
        }
        sd = "Porta secreta presa. Role na tabela de armadilhas. Do outro lado há uma "+temp;
    }
    return sd;
}
