function sdRandomizer() {
    rand = minMaxRandomizer(1, 6);
    var sd = "";
    if (rand < 3) {
        rndInt[7] += 40;
        sd = "A porta secreta se abre para uma sala. "+roomRandomizer();
    } else if (rand > 2 && rand < 5) {
        rndInt[1] = minMaxRandomizer(1, 100);
        rndInt[3] += 40;
        sd = "A porta secreta abre para a passagem."+passageRandomizer();
    }  else {
        temp1 = minMaxRandomizer(1, 100);
        var temp = "";
        if (temp1 < 51) {
            rndInt[1] = minMaxRandomizer(1, 100);
            rndInt[3] += 50;
            temp += "passagem. "+passageRandomizer();
        } else {
            rndInt[7] += 50
            temp += "sala. "+roomRandomizer();
        }
        sd = "Porta secreta presa. "+trapRandomizer()+". Do outro lado há uma "+temp;
    }
    return sd;
}
