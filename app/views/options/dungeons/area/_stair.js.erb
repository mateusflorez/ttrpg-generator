function stairRandomizer() {
    var stair = "";
    if (rndInt[5] < 9) {
        temp1 = minMaxRandomizer(1, 4);
        var temp2 = "";
        if (temp1 < 3) {
            rndInt[1] += 1;
            temp2 = "passagem. "+passageRandomizer();
        } else {
            temp2 = "sala. "+roomRandomizer();
        }
        stair = "Escada. Desce um nível até uma "+temp2;
    } else if (rndInt[5] === 9) {
        rndInt[7] += 15;
        stair = "Escada descendo um nível para uma sala. "+roomRandomizer();
    }  else if (rndInt[5] === 10) {
        rndInt[7] += 30;
        stair = "Escada descendo um nível para uma sala. "+roomRandomizer();
    }  else if (rndInt[5] === 11) {
        rndInt[1] += 1;
        stair = "Escada. Desce um nível para uma passagem. "+passageRandomizer();
    }  else if (rndInt[5] === 12) {
        rndInt[1] += 1;
        rndInt[3] += 15;
        stair = "Escada. Desce um nível para uma passagem. "+passageRandomizer();
    }  else if (rndInt[5] === 13) {
        rndInt[1] += 1;
        rndInt[3] += 30;
        stair = "Escada. Desce um nível para uma passagem. "+passageRandomizer();
    }  else if (rndInt[5] === 14) {
        temp1 = minMaxRandomizer(1, 4);
        var temp2 = "";
        if (temp1 < 3) {
            rndInt[1] += 1;
            temp2 = "passagem. "+passageRandomizer();
        } else {
            temp2 = "sala. "+roomRandomizer();
        }
        stair = "Escada descendo dois níveis até uma "+temp2;
    }  else if (rndInt[5] === 15) {
        rndInt[3] += 15;
        stair = "Escada subindo um nível para uma sala. "+roomRandomizer();
    }  else if (rndInt[5] === 16) {
        rndInt[3] += 30;
        stair = "Escada subindo um nível para uma sala. "+roomRandomizer();
    }  else if (rndInt[5] === 17) {
        rndInt[1] += 1;
        stair = "Escada. Sobe um nível para uma passagem. "+passageRandomizer();
    }  else if (rndInt[5] === 18) {
        rndInt[1] += 1;
        rndInt[3] += 15;
        stair = "Escada. Sobe um nível para uma passagem. "+passageRandomizer();
    }  else if (rndInt[5] === 19) {
        rndInt[1] += 1;
        rndInt[3] += 30;
        stair = "Escada. Sobe um nível para uma passagem. "+passageRandomizer();
    }  else {
        temp1 = minMaxRandomizer(1, 4);
        var temp2 = "";
        if (temp1 < 3) {
            rndInt[1] += 1;
            temp2 = "passagem. "+passageRandomizer();
        } else {
            temp2 = "sala. "+roomRandomizer();
        }
        stair = "Escada subindo dois níveis até uma "+temp2;
    }
    return stair;
}
